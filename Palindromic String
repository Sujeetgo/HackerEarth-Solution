#include<iostream>
using namespace std;
int main(){
	string s;
	cin>>s;
	int n = s.length();
	string temp = "";
	for(int i = n - 1; i>=0; i--){
		temp +=s[i];
	}
	if(temp==s){
		cout<<"YES";
	}
	else{
		cout<<"NO";
	}

}
